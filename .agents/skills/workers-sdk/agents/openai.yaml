interface:
  display_name: "Workers Sdk"
  short_description: "Repo-specific patterns and workflows for workers-sdk"
  default_prompt: "Use the workers-sdk repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true